# # services:
# #   - type: web
# #     name: clickconnect-backend
# #     env: python
# #     buildCommand: pip install -r requirements.txt
# #     startCommand: gunicorn website_backend.wsgi:application

# services:
#   - type: web
#     name: backend
#     env: python
#     buildCommand: |
#       pip install -r requirements.txt &&
#       python manage.py makemigrations &&
#       python manage.py migrate &&
#       python create_superuser.py &&
#       python manage.py collectstatic --noinput
#     startCommand: gunicorn website_backend.wsgi:application
services:
  - type: web
    name: backend
    env: python
    buildCommand: |
      pip install -r requirements.txt &&
      python manage.py makemigrations &&
      python manage.py migrate &&
      python create_superuser.py &&
      python manage.py collectstatic --noinput
    startCommand: gunicorn website_backend.wsgi:application
